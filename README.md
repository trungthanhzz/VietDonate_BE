# VietDonate_BE

# Guildane to setup postgresql by docker ( MUST be install docker, docker compose before)

    - Step 1: Cd to BE localtion
    - Step 2: open cmd and copy & paste this command
        docker compose -f docker-compose.postgres.yml up -d
    - Step 3: wait process and change the conection string in appsettings.json (BE)
